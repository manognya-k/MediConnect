# Frontend

This folder is reserved for the frontend application.

Suggested setup options:

- Angular (`ng new frontend-app`)
- React (`npm create vite@latest`)

After creating the app, keep all UI code inside this folder.
